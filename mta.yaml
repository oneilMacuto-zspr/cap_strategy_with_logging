_schema-version: 3.3.0
ID: cap_strategy_001
description: A simple CAP project.
version: 1.0.0
modules:
- name: cap_strategy_001-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: hana-hdi-001
  - name: cap-dev-destination-001
  - name: cap-dev-xsuaa-001
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: cap_strategy_001-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: hana-hdi-001
  parameters:
    buildpack: nodejs_buildpack
- name: cap_strategy_001-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: cap_strategy_001-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - adminappadminapp.zip
      name: adminappadminapp
      target-path: resources/
- name: adminappadminapp
  type: html5
  path: app/admin-app
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: hana-hdi-001
  type: org.cloudfoundry.existing-service
- name: cap-dev-destination-001
  type: org.cloudfoundry.existing-service
- name: cap-dev-xsuaa-001
  type: org.cloudfoundry.existing-service
- name: cap_strategy_001-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: cap_strategy_001-html5-service
    service-plan: app-host
- name: cap_strategy_001-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: cap_strategy_001-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
